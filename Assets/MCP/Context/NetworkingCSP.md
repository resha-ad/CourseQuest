# Networking Content Security Policy — CourseQuest VR

**Not applicable** — see `NetworkingGuidelines.md`. The only outbound network calls are to Unity
Gaming Services (Cloud Save + Authentication) endpoints, configured through the Unity Services SDK
itself rather than any custom endpoint list.
